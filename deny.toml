[licenses]
allow = ["Apache-2.0", "MIT", "Unicode-3.0"]
private = { ignore = true }

[advisories]
yanked = "deny"
ignore = [
    # bincode is deprecated -- just a dev-dependency in this crate
    "RUSTSEC-2025-0141",
    # proc-macro-error2 is unmaintained, via defmt
    "RUSTSEC-2026-0173",
]
